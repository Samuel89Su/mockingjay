'use strict';

const ValidationConst = {
    validatorTypes: {
        exact: 'exact',
        regEx: 'regEx',
        custom: 'custom',
        jsonSchema: 'jsonSchema'
    },

    valReactorTypes: {
        fixed: 'fixed',
        custom: 'custom'
    },

    
}

exports = module.exports = ValidationConst